rules_version = '2';

// 認証なしの社内ツール向け。API キー流出時は誰でも読み書き可能です。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
